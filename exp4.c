#include <stdio.h>`
int main() {
    int number, quotient;
    printf("Enter a number: ");
    scanf("%d", &number);
    quotient = number / 6;
    printf("Quotient after dividing by 6: %d\n", quotient);
    return 0;
}

